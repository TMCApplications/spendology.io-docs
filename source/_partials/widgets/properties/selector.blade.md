The selector to use to find out where to inject the widgets. If the selector is an id (recommended) then prefix with a
`#` (e.g. `#WidgetName`) or if the selector is a class then prefix with a `.`
(e.g. `.WidgetName`).

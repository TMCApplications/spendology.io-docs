This will show two dropdown boxes to capture where and when the customer is going on their trip.

<blockquote>
    This must be a boolean <code>true</code> or <code>false</code> value only.
</blockquote>
